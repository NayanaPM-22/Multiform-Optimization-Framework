# Multiform Optimization Framework (MFO)
### For Constrained Multiobjective Optimization (CMOPs)

This README explains all steps from setting up the framework to running the Adaptive Knowledge Transfer (AKT) algorithm and viewing results.

## 1. Environment Setup
Install the necessary dependencies to run the optimization:
```bash
pip install pymoo>=0.6.0 matplotlib numpy pandas
